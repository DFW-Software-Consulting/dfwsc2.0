FROM node:20-alpine AS development

ENV NODE_ENV=development
WORKDIR /app

COPY package*.json ./
RUN npm ci

# Vite dev server will be started with source mounted as volumes
EXPOSE 5173

# This will be overridden by docker-compose
CMD ["npm", "run", "dev", "--", "--host"]
