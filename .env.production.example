# ============================================
# Production Environment Variables for Coolify
# ============================================
# Copy these to Coolify's environment variable section
# DO NOT commit actual secrets to git!

# ============================================
# PostgreSQL Database
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=<CHANGE_ME_SECURE_PASSWORD>
POSTGRES_DB=stripe_portal

# ============================================
# Stripe Configuration
# ============================================
# Use production keys for live payments, test keys for testing
# Format: sk_live_... or sk_test_...
STRIPE_SECRET_KEY=<YOUR_STRIPE_SECRET_KEY>
# Format: pk_live_... or pk_test_...
STRIPE_PUBLISHABLE_KEY=<YOUR_STRIPE_PUBLISHABLE_KEY>
# Format: whsec_...
STRIPE_WEBHOOK_SECRET=<YOUR_STRIPE_WEBHOOK_SECRET>

# ============================================
# JWT Authentication
# ============================================
# Generate: openssl rand -base64 48
JWT_SECRET=<GENERATE_SECURE_64_CHAR_STRING>
JWT_EXPIRY=1h

# Admin credentials for API access
ADMIN_USERNAME=admin
# For production, use bcrypt hashed password:
# node -e "console.log(require('bcrypt').hashSync('your_password', 10))"
ADMIN_PASSWORD=<CHANGE_ME_SECURE_PASSWORD>

# ============================================
# SMTP Email Configuration
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=<YOUR_APP_PASSWORD>
SMTP_FROM=noreply@dfwsc.com

# ============================================
# Payment Configuration
# ============================================
USE_CHECKOUT=true
DEFAULT_PROCESS_FEE_CENTS=100

# ============================================
# Auto-configured by docker-compose.coolify.yml
# ============================================
# These are set automatically, no need to configure in Coolify:
# - NODE_ENV=production
# - PORT=4242
# - DATABASE_URL (constructed from POSTGRES_* vars)
# - FRONTEND_ORIGIN=https://stripe.dfwsc.com
# - VITE_API_URL=/api/v1
