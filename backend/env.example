# Stripe credentials
STRIPE_SECRET_KEY=sk_test_************************
STRIPE_WEBHOOK_SECRET=whsec_12345

# Application runtime
PORT=4242
FRONTEND_ORIGIN=http://localhost:5173
USE_CHECKOUT=false
# Optional: set when the API is reachable via a public URL that differs from the host header
# API_BASE_URL=http://localhost:4242

# Database connection string used by Drizzle ORM and the application
DATABASE_URL=postgres://postgres:postgres@localhost:5432/stripe_payment_portal

# SMTP Configuration (placeholders)
SMTP_HOST=placeholder.smtp.com
SMTP_PORT=587
SMTP_USER=user@placeholder.com
SMTP_PASS=your_placeholder_password

# The base URL of the API, e.g., http://localhost:4242 or your ngrok URL
API_BASE_URL=
