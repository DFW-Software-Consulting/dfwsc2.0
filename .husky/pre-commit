#!/usr/bin/env sh

# 1. Auto-fix staged files with Biome
npx lint-staged

# 2. Frontend tests (no Docker required)
npm run test:front

# 3. Backend tests: run inside Docker container if the stack is up, otherwise warn and skip
COMPOSE="docker compose -f docker-compose.base.yml -f docker-compose.dev.yml"

if $COMPOSE ps --status running 2>/dev/null | grep -q "api"; then
  echo "Docker stack is running — running backend tests inside container..."
  $COMPOSE exec api npm test
else
  echo ""
  echo "⚠️  Docker stack not running — backend tests skipped."
  echo "    Run 'make test' or 'make test-up' before pushing to verify."
  echo ""
fi
