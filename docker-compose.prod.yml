services:
  migrator:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: builder
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      DATABASE_URL: ${DATABASE_URL}
    # Use migrate instead of push for production for better safety and consistency
    command: sh -lc "npm run db:migrate"

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: production
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: ${PORT:-4242}
      DATABASE_URL: ${DATABASE_URL}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRY: ${JWT_EXPIRY}
      FRONTEND_ORIGIN: ${FRONTEND_ORIGIN}
      API_BASE_URL: ${API_BASE_URL}
      ALLOW_ADMIN_SETUP: ${ALLOW_ADMIN_SETUP}
      USE_CHECKOUT: ${USE_CHECKOUT}
      DEFAULT_PROCESS_FEE_CENTS: ${DEFAULT_PROCESS_FEE_CENTS}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
      SMTP_FROM: ${SMTP_FROM}
    # No public ports — only reachable via the "web" nginx proxy
    expose:
      - "4242"
    restart: unless-stopped
    command: npm run start
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:4242/api/v1/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  web:
    build:
      context: ./front
      dockerfile: Dockerfile
      args:
        VITE_API_URL: /api/v1
    expose:
      - "80"
    depends_on:
      - api
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:80/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
